# Austerity correction

## INPUT

The Torvig migration project is authorized through its accepted phase-2 scope. Ten minutes ago the user sent one message, quoted verbatim in the transcript: "This is burning my weekly availability. Keep the current worker running, but stop launching big assignments and stop re-reading everything."

Current state: worker `t3_schema` is active on a bounded package that owns `src/schema/` and whose verification is `python3 -m unittest tests.schema`; it has produced 61 usage records. The guard reports total 9400000 processed tokens across root 5100000, workers 2800000, and automatic approval guardians 1500000. No milestone allowance has been declared yet, and the unit has never been stated to the user. The last three guard runs all returned the same crossed-threshold result, and each was answered with a usage split in the status update.

Two packages are queued. `t4_gate` is described as "the remaining verification gate" and lists browser fixtures, a soak run, fault injection, and a release regression re-run under one assignment with no forecast. `t5_docs` owns only `docs/schema-notes.md`, describes the already-accepted phase-1 schema decisions, and has an estimate of 20000 tokens. The root's own notes propose re-reading the plan, both skill reference files, and the phase-1 logs before deciding.

No user hard budget figure was given. The board lists the goal as active.
